<%= form_with(model: hotel, local: true) do |form| %>
  <% if hotel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hotel.errors.count, "error") %> prohibited this hotel from being saved:</h2>

      <ul>
      <% hotel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hotelname %>
    <%= form.text_field :hotelname %>
  </div>

  <div class="field">
    <%= form.label :hoteladdress %>
    <%= form.text_field :hoteladdress %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
